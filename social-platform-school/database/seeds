<?php
// This file is intended to seed the database with initial data for the social platform.
// You can add your seed data here for users, posts, comments, etc.

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    public function run()
    {
        // Call individual seeders here
        $this->call(UserSeeder::class);
        $this->call(PostSeeder::class);
        $this->call(CommentSeeder::class);
        $this->call(EventSeeder::class);
        // Add more seeders as needed
    }
}

class UserSeeder extends Seeder
{
    public function run()
    {
        // Seed users
        DB::table('users')->insert([
            ['name' => 'Admin User', 'email' => 'admin@example.com', 'password' => bcrypt('password'), 'role' => 'admin'],
            ['name' => 'Student User', 'email' => 'student@example.com', 'password' => bcrypt('password'), 'role' => 'student'],
            // Add more users as needed
        ]);
    }
}

class PostSeeder extends Seeder
{
    public function run()
    {
        // Seed posts
        DB::table('posts')->insert([
            ['user_id' => 1, 'caption' => 'Welcome to the new school year!', 'created_at' => now()],
            // Add more posts as needed
        ]);
    }
}

class CommentSeeder extends Seeder
{
    public function run()
    {
        // Seed comments
        DB::table('comments')->insert([
            ['post_id' => 1, 'user_id' => 2, 'comment' => 'Excited for the new year!', 'created_at' => now()],
            // Add more comments as needed
        ]);
    }
}

class EventSeeder extends Seeder
{
    public function run()
    {
        // Seed events
        DB::table('events')->insert([
            ['title' => 'School Open Day', 'date' => '2025-09-01', 'description' => 'Join us for the school open day!', 'created_at' => now()],
            // Add more events as needed
        ]);
    }
}
?>